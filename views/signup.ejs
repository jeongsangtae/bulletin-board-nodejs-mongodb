<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('includes/head', { title: "가입하기" }) %>
    <link rel="stylesheet" href="/styles/auth-form.css" />
    <link rel="stylesheet" href="/styles/auth.css" />
  </head>

  <body>
    <%- include('includes/header') %>
    <form
      action="/signup"
      method="POST"
      class="main-container"
      id="main-write-container"
    >
      <div class="auth-form">
        <div class="main-title">
          <h1>가입하기</h1>
          <% if (inputData.hasError) { %>
          <p class="input-error"><%= inputData.message %></p>
          <% } %>
        </div>

        <div class="board-write-container">
          <div class="board-write">
            <div class="write-title">
              <label for="email">이메일</label>
              <input
                type="email"
                id="email"
                required
                name="email"
                value="<%= inputData.email %>"
                placeholder="이메일 입력"
              />
            </div>
            <div>
              <label for="confirm-email">이메일 확인</label>
              <input
                type="email"
                id="confirm-email"
                required
                name="confirm-email"
                value="<%= inputData.confirmEmail %>"
                placeholder="이메일 확인 입력"
              />
            </div>
            <div>
              <label for="name">이름</label>
              <input
                type="text"
                id="name"
                required
                name="name"
                value="<%= inputData.name %>"
                placeholder="이름 입력"
              />
            </div>
            <div>
              <label for="password">비밀번호</label>
              <input
                type="password"
                id="password"
                required
                name="password"
                value="<%= inputData.password %>"
                placeholder="비밀번호 입력"
              />
            </div>
          </div>
        </div>
        <div class="btn-wrap">
          <button class="btn-add">가입</button>
          <a href="/" class="btn-cancel">취소</a>
        </div>
        <div class="auth-alternative">
          <a href="/login" class="btn-auth">로그인 하러가기</a>
        </div>
      </div>
    </form>
  </body>
</html>
