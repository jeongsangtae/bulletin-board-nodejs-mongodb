<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('includes/head', { title: "게시판" }) %>
    <link rel="stylesheet" href="/styles/board.css" />
  </head>

  <body>
    <%- include('includes/header') %>
    <div class="main-container">
      <div class="main-title">
        <h1>게시판</h1>
        <p>게시글 내용을 확인해보세요.</p>
      </div>
      <div class="board-list-container">
        <div class="board-list-top">
          <div class="board-top">
            <div class="board-num">번호</div>
            <div class="board-title">제목</div>
            <div class="board-writer">글쓴이</div>
            <div class="board-date">작성일</div>
            <div class="board-count">조회</div>
          </div>
        </div>
        <% if (!posts || posts.length === 0) { %>
        <p>게시글이 없습니다. 게시글을 추가해보세요.</p>
        <% } else { %>
        <div class="board-list" id="board-list-table">
          <% for(const post of posts) { %> <%- include('includes/post', { post:
          post }) %> <% } %>
        </div>
        <div class="board-page" id="board-list-page"></div>
        <% } %>
        <div class="btn-wrap">
          <a href="/create-post" class="btn-add">등록</a>
        </div>
      </div>
    </div>
    <script src="public/scripts/write.js" defer></script>
  </body>
</html>
